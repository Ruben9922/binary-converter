import { FormControl, FormControlProps } from "@chakra-ui/react";
import { motion } from "framer-motion";

const MotionFormControl = motion<FormControlProps>(FormControl);

export default MotionFormControl;
